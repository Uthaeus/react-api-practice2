import { useState, useEffect } from "react";

function Meetups() {
    const [meetups, setMeetups] = useState([]);

    useEffect(() => {

    }, []);

    return (
        <div>
            <h1>Meetups Page</h1>
        </div>
    );
}

export default Meetups;